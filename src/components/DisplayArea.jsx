import useCalendar from '@veccu/react-calendar';

const DisplayArea = () => {
    return (
        <div className="flex-1 text-xs">
            <pre>Display</pre>
        </div>
    );
};

export default DisplayArea;
