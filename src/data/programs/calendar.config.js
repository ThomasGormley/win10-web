import { createProgramConfig } from '../../util/createProgramConfig';

export const calendarProgramConfig = createProgramConfig({
    id: 'calendar',
    tooltip: 'Calendar',
    title: 'Calendar',
});
