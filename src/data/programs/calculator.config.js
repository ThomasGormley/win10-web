import { createProgramConfig } from '../../util/createProgramConfig';

export const calculatorProgramConfig = createProgramConfig({
    id: 'calculator',
    tooltip: 'Calculator',
    title: 'Calculator',
});
