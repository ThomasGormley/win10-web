import { createProgramConfig } from '../../util/createProgramConfig';

export const msstoreProgramConfig = createProgramConfig({
    id: 'ms-store',
    tooltip: 'Microsoft Store',
    title: 'Microsoft Store',
});
